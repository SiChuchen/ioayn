# Skill evaluation cases

`cases.json` records initial behavioral checks. Run each prompt in a fresh repository session with the plugin enabled and disabled, then compare whether the assertions hold.

For automated Claude Code skill evaluation, the official `skill-creator` plugin can be used to convert or expand these cases into its current evaluation format.
