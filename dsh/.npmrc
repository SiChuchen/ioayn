# @deepseek-ai/dsh-agent-presets@0.0.1-rc.1 declares a non-optional peer on
# @deepseek-ai/dsh-paths, which is not published to npm (404 as of 2026-08).
# The dsh harness provides these peer services at runtime, so skip npm's
# peer auto-install.
legacy-peer-deps=true
